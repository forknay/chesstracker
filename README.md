Open `index.html` in any browser

Data refreshes automatically every 2 minutes (profiles/stats) and 10 minutes (game archives). Archive data is cached in localStorage.

## Players tracked

I just track my friends and I, you can edit the `PLAYERS` array at the top of the `<script>` block in `index.html` to change the list.
